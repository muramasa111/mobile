# Flutter学習ログ

## 2026-05-20 14:02 +09:00

### 今日扱った内容
- Flutterでは画面をWidgetの組み合わせで作ることを確認した。
- `Scaffold`、`Text`、`ElevatedButton`、`Column`、`Row`、`Container`、`Navigator` などの基本Widgetを確認した。
- データ構造はDartの `class` で作ることを確認した。
- `Star` クラスを作成した。
- `required this.xxx` は、クラスを作るときに必ず指定する値を表す書き方だと確認した。

### 現在の進捗
- `lib/models/star.dart` に `Star` クラスを作成済み。
- 次は星座線を表すクラスを作る予定。

### 理解したこと
- `class` はデータの設計図。
- `final` は一度決めたら変えない値。
- `String` は文字、`double` は小数。
- `required this.id` は、作成時に `id` を必ず渡すという意味。

## 2026-05-20 14:07 +09:00

### 今日扱った内容
- 星座線を表す `ConstellationEdge` クラスを作成した。
- `Constellation` は星座、`Edge` は線やつながりという意味だと確認した。
- Dartの変数名は `fromStarID` より `fromStarId` のようなキャメルケースが自然だと確認した。

### 現在の進捗
- `lib/models/star.dart` に `Star` クラスを作成済み。
- `lib/models/constellation_edge.dart` に `ConstellationEdge` クラスを作成済み。
- 次は星座全体を表す `Constellation` クラスを作る予定。

## 2026-05-20 14:53 +09:00

### 今日扱った内容
- `StarView` に `CustomPaint` を配置した。
- `StarPainter extends CustomPainter` を作成した。
- `paint` メソッド内で `canvas.drawCircle` を使い、星のような丸を描画した。
- 色を `Colors.red` にして、画面上に丸が表示されることを確認した。

### 理解したこと
- `CustomPaint` は自由に図形を描くためのWidget。
- `CustomPainter` は実際の描画処理を書くクラス。
- `canvas.drawCircle` で丸を描ける。
- `Offset(x, y)` で描画位置を指定する。

## 2026-05-20 15:15 +09:00

### 今日扱った内容
- `StarPainter` に `Constellation` データを渡すようにした。
- `constellations[0]` から星座データを1つ取り出した。
- `for (final star in constellation.stars)` で星のリストを順番に処理した。
- `star.x`、`star.y` を使って、データに登録した星を画面に描画した。

### 現在の進捗
- `constellation_data.dart` の星データから、画面に点を2つ表示できた。

## 2026-05-20 15:48 +09:00

### 今日扱った内容
- `StarView` を `StatelessWidget` から `StatefulWidget` に変更した。
- `StatefulWidget` では `createState()` を使って、状態を管理する `State` クラスを作ることを確認した。
- `StatefulWidget` は大文字小文字を正確に書く必要があることを確認した。

### 現在の進捗
- 星をタップして選択する準備として、`StarView` が状態を持てる形になった。

## 2026-05-27 16:32 +09:00

### 実装した内容
- タップ位置から半径16以内の星を探す `_findTappedStar` を追加した。
- 選択中の星を保持する `_selectedStar` を追加した。
- 星をタップしたとき、`setState` で選択状態を更新するようにした。
- `StarPainter` に選択中の星を渡し、選択された星だけ黄色かつ大きく描画するようにした。

### 理解のポイント
- `Star?` は、星が入る場合と `null` の場合がある変数。
- `setState` の中で値を変えると、画面が再描画される。
- `shouldRepaint` で選択中の星が変わったときに再描画するよう判断できる。

## 2026-06-03 13:44 +09:00

### 実装した内容
- 2個目の星をタップしたとき、前に選択していた星との組み合わせを認識する処理を追加した。
- `_handleTappedStar` を追加し、星タップ時の処理をまとめた。
- 前回選択した星と今回タップした星が違う場合、`debugPrint` で組み合わせを表示するようにした。

### 現在の進捗
- 星をタップして選択できる。
- 2個目の星をタップしたとき、`前の星 -> 今の星` の組み合わせを認識できる。
- 正しい組み合わせかどうかの判定と線の追加は、まだ未実装。

## 2026-06-03 16:34 +09:00

### 実装した内容
- 2つの星の組み合わせが正しい星座線か判定する `_isCorrectPair` を追加した。
- `constellation.edges` を順番に確認し、タップした2つの星IDが登録済みの線と一致するか調べるようにした。
- 星座線は向きに関係なく判定するため、`A -> B` と `B -> A` の両方を正解扱いにした。
- 正しい組み合わせなら `Correct pair`、間違いなら `Wrong pair` をデバッグ出力するようにした。

### 現在の進捗
- 星をタップして選択できる。
- 2個目の星をタップしたとき、正しい組み合わせかどうか判定できる。
- 正解時に線を追加して描画する処理は、まだ未実装。

## 2026-06-10 13:46 +09:00

### 実装した内容
- 正しい星同士をタップしたとき、対応する星座線を `_connectedEdges` に保存する処理を追加した。
- 同じ星座線を重複して追加しない `_isAlreadyConnected` を追加した。
- `StarPainter` に接続済みの線リストを渡し、`canvas.drawLine` で線を描画するようにした。
- 線を描くために、星IDから星データを探す `_findStarById` を追加した。

### 現在の進捗
- 星をタップして選択できる。
- 2個目の星をタップしたとき、正しい組み合わせかどうか判定できる。
- 正しい組み合わせなら、星同士を線でつなげる。
- 星座完成判定は、まだ未実装。

## 2026-06-10 13:53 +09:00

### 実装した内容
- 接続済みの線が星座に定義された線の数と同じになったら、星座完成と判定する処理を追加した。
- `_isConstellationCompleted` を追加し、完成メッセージが何度も出ないようにした。
- 完成した瞬間に `SnackBar` で「こと座 完成！」のようなメッセージを表示するようにした。

### 現在の進捗
- 星をタップして選択できる。
- 2個目の星をタップしたとき、正しい組み合わせかどうか判定できる。
- 正しい組み合わせなら、星同士を線でつなげる。
- 星座の全ての線をつなぐと、完成メッセージを表示できる。

## 2026-06-10 15:32 +09:00

### 実装した内容
- `ConstellationList` の仮テキストを、星座データ一覧表示に変更した。
- `constellation_data.dart` を読み込み、`constellations` の中身を `ListView.builder` で表示するようにした。
- 各星座を `ListTile` で表示し、タイトルに星座名、サブタイトルに説明文を出すようにした。

### 現在の進捗
- 星座一覧画面に、登録済みの星座データを表示できる。
- 図鑑の完成状態や未発見表示は、まだ未実装。

## 2026-06-17 13:45 +09:00

### 実装した内容
- 星座一覧画面の項目をタップすると、星座詳細画面へ遷移する処理を追加した。
- `ConstellationDetail` に `Constellation` データを渡せるようにした。
- 詳細画面で星座名、説明、星の数、星座線の数を表示するようにした。
- `HomePage` から引数なしで詳細画面を開いても落ちないように、`constellation` は任意引数にした。

### 現在の進捗
- 星座一覧に登録済み星座を表示できる。
- 一覧から星座詳細画面へ移動できる。
- 詳細画面に選択した星座の基本情報を表示できる。

## 2026-06-17 14:30 +09:00

### 実装した内容
- 星座詳細画面に、その星座に含まれる星の一覧を表示するようにした。
- `selectedConstellation.stars` を `for` で順番に取り出し、星名を表示する処理を追加した。

### 現在の進捗
- 星座詳細画面で、星座名、説明、星の数、星座線の数、含まれる星を確認できる。

## 2026-06-24 13:44 +09:00

### 実装した内容
- アプリ起動中の星座完成状態を管理する `progress_data.dart` を追加した。
- 星座完成時に `markConstellationCompleted` で完成済みIDを登録するようにした。
- 星座一覧画面で、完成済みの星座に「完成済み」とチェックアイコンを表示するようにした。

### 現在の進捗
- 星座が完成すると、図鑑一覧で完成済みとして表示できる。
- 完成状態の永続保存は、まだ未実装。

## 2026-06-24 16:47 +09:00

### 実装した内容
- `shared_preferences` を追加し、星座の完成状態を保存できるようにした。
- `progress_data.dart` に `loadProgress` を追加し、アプリ起動時に保存済みの完成済み星座IDを読み込むようにした。
- `markConstellationCompleted` で完成済み星座IDを保存するようにした。
- `main` を非同期にし、`runApp` の前に `loadProgress` を実行するようにした。

### 確認したこと
- `flutter pub get` は成功した。
- `flutter analyze` は実行できたが、既存のファイル名やWidget key指定などのstyle infoが残っている。
- 今回追加した保存処理自体の構文エラーは出ていない。

### 現在の進捗
- 星座が完成すると、図鑑一覧で完成済みとして表示できる。
- 完成済み状態をアプリ再起動後も読み込める形になった。

## 2026-07-01 13:38 +09:00

### 実装した内容
- 星座の進捗を「未発見」「名前解禁済み」「完成済み」の3段階で管理するようにした。
- 最初の正しい星座線がつながった時点で、星座を名前解禁済みとして保存するようにした。
- 図鑑一覧では、未発見の星座名を `???` と表示するようにした。
- 完成するまでは詳細画面を開けないようにした。

### 確認したこと
- `dart format` で変更した3ファイルを整形した。
- `flutter analyze` では既存のファイル名やWidgetのkeyなど、17件の警告・style infoのみが表示された。
- 今回追加した3段階進捗処理に構文エラーはない。

### 現在の進捗
- 星座を1本つなぐと名前が解禁され、すべてつなぐと詳細を見られる。
- 名前解禁状態と完成状態は、アプリ再起動後も保持される。

## 2026-07-01 16:51 +09:00

### 実装した内容
- 星空画面を指やマウスでドラッグして動かせるようにした。
- `_viewOffset` にドラッグによる移動量を保存するようにした。
- `canvas.translate` で星と星座線をまとめて移動するようにした。
- タップ位置から移動量を引き、移動後も星を正しく選択できるようにした。

### 確認したこと
- `dart format` で `StarView.dart` を整形した。
- `flutter analyze` では既存の17件の警告・style infoのみで、今回の処理に構文エラーはない。

### 現在の進捗
- 画面ドラッグによる星空の移動と、移動後の星の選択ができる。
## 2026-07-08 10:15 +09:00

- スマホセンサー以外の主要機能を追加した。
- 星座データを、やぎ座・こと座・わし座・白鳥座の4つに増やした。
- 星空ガイドビューで、表示する星座を切り替えられるようにした。
- 間違った星同士を選んだ時、選んだ星が一瞬暗くなる反応を追加した。
- 星座完成時に「図鑑に登録されました」と表示するようにした。
- 図鑑で、未発見・名前解禁済み・完成済みの状態を表示できるようにした。
- 星座詳細に、代表的な星・季節・方角・神話・豆知識を表示できるようにした。

## 2026-07-08 16:47 +09:00

- 星空ガイドビューの画面下に、半円の方角メーターを追加した。
- 画面ドラッグの横方向の移動量から、仮の方角を計算して表示するようにした。
- メーターには現在方向、角度、N/E/Wの目印、中央の針を表示した。
## 2026-07-14 15:17 +09:00

- 星座表示を横方向に循環させた。ドラッグで空を動かし続けると、360度分の仮想的な空を一周して同じ星空に戻る。
- 星の描画・星座線の描画・タップ判定に同じ横方向の循環ルールを適用し、表示位置とタップ位置がずれないようにした。
## 2026-07-14 15:26 +09:00

- 星空を横360度・縦180度の仮想的な天球として扱うように変更した。
- 実装する4つの星座を天球上の別々の位置へ配置し、ドラッグで周囲の空を見回せるようにした。
- 選択中の星座だけをタップ対象にし、他の星座と背景の星は見た目だけ表示する。
## 2026-07-14 15:37 +09:00

- 天球上に表示されている実装済みのすべての星をタップ対象に変更した。
- 接続した線を星座ごとに記録するため、別の星座へ移動しても接続状況が消えないようにした。
## 2026-07-14 15:58 +09:00

- 星座ごとの接続線リストを `List<ConstellationEdge>` として明示し、Web版で起きた型変換エラーを修正した。
## 2026-07-14 16:03 +09:00

- こと座・わし座・白鳥座を完成したあと、ベガ・アルタイル・デネブをつないで夏の大三角を発見する機能を追加した。
- 夏の大三角の発見状態を保存し、星座図鑑の特別発見として詳細を表示できるようにした。
## 2026-07-14 16:09 +09:00

- 夏の大三角の特別発見に使う線を、通常の星座線の黄色ではなく青色で描画するように変更した。
## 2026-07-14 16:12 +09:00

- ホーム画面から星座詳細へ直接移動するボタンを削除した。詳細は星座図鑑の完成済み項目から開く。
## 2026-07-14 16:21 +09:00

- 方位計の目盛りとN・E・S・Wの表示が、現在の方角に合わせて回転するように変更した。
- 中央の黄色い針を削除し、中央には現在の方角と角度だけを表示するようにした。

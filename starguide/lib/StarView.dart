import 'package:flutter/material.dart';
//import '../sample/SecondPage.dart';

class StarView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("天体観測")),
      body: Center(child: Text("Star View")),
    );
  }
}

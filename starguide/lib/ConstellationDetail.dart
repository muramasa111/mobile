import 'package:flutter/material.dart';

class ConstellationDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("星座詳細")),
      body: Center(child: Text("Constellation Detail")),
    );
  }
}

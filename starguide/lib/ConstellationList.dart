import 'package:flutter/material.dart';

class ConstellationList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("星座一覧")),
      body: Center(child: Text("Constellation List")),
    );
  }
}
